<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.homework.mapper.PicMapper">
	<select id="getOnePic" parameterType="string" resultType="picDto">
		SELECT * FROM kdt_pic_info_basc WHERE pic_name=#{keyword}
	</select>
</mapper>